# Server-only runtime dependencies.
# The root requirements.txt is installed first; this file adds the web
# and worker dependencies on top.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic-settings>=2.2
celery[redis]>=5.3
redis>=5.0

# Async HTTP client for the provider model-list proxy
# (server/services/model_list_proxy.py).
httpx>=0.27

# Server-sent events for the live run-log stream
# (server/routes/logs.py).
sse-starlette>=2.1

# Process manager for running uvicorn + celery in a single container
# (server/supervisord.conf, used by Render's dockerCommand).
supervisor>=4.2
