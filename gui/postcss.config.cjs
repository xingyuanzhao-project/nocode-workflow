/**
 * PostCSS configuration loaded by Vite when processing globals.css.
 *
 * Tailwind compiles utility classes and autoprefixer adds vendor
 * prefixes. Both plugins are expected by the shadcn/ui workflow.
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
