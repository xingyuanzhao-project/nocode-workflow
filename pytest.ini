[pytest]
minversion = 7.0
addopts = -ra --strict-markers --strict-config
testpaths =
    tests
markers =
    integration: requires docker compose up and an OpenRouter API key; never runs by default.
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:celery.*
    ignore::DeprecationWarning:kombu.*
