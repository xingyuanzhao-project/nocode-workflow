{
  "summary": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "relevant_context": "<list of context keys found, or empty list>",
      "summary": "<texto in spanish>"
    },
    "instructions": [
      "If the input is an error/missing page (e.g., \"Página no encontrada\", \"404\", \"no se puede encontrar esa página\"), set info_found=\"FALSE\", relevant_context=[], summary=\"\"",
      "Ignore navigation/site chrome (menú, buscar, categorías, compartir, ThemeGrill, WordPress, cookies, copyright)",
      "relevant_context should list the keys from the related_context in that are found in the text (e.g., [\"vic_grupo_social\", \"captura_metodo\", \"perp_tipo1\"])",
      "Extractive summary in Spanish: copy exact spans; DO NOT paraphrase; preserve modality (\"soñaba ser\", \"quería ser\", \"aspiraba a\")",
      "If no relevant info, relevant_context=[] and summary=\"\"",
      "NO APOLOGIES, NO FILLER TEXT"
    ]
  },
  "summary_first": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "relevant_context": ["<label keys found>"],
      "summary_by_item": {
        "<label_key>": [
          {"span": "<exact SENTENCE from input text>"}
        ]
      },
      "summary": "<text in spanish>"
    },
    "instructions": [
      "If the input is an error/missing page (e.g., \"Página no encontrada\", \"404\", \"no se puede encontrar esa página\"), set {info_found_label}=\"FALSE\", {relevant_context_label}=[], summary_by_item={{}}, {summary_label}=\"\".",
      "Ignore navigation/site chrome (menú, buscar, categorías, compartir, ThemeGrill, WordPress, cookies, copyright).",
      "Use only the current {input_text_label} and {related_context_label} to decide {info_found_label}, {relevant_context_label}, summary_by_item and {summary_label}.",
      "relevant_context should list the keys from {related_context_label} that are found in the text (e.g., [\"vic_grupo_social\", \"captura_metodo\", \"perp_tipo1\"]).",
      "For each key X in {relevant_context_label}, check if there is any information about X in {input_text_label}. If yes, add one bullet to {summary_label} for X, copying the exact Spanish span(s) from {input_text_label} about X (no paraphrasing; preserve modality).",
      "For each key X in {relevant_context_label}, extract the exact Spanish span(s) from {input_text_label} about X and store them in summary_by_item[X] as a list of objects with 'span' (the exact text).",
      "Each span must be a full original sentence ending in sentence punctuation (., ?, !, or equivalent) with no truncation or stitching; if you cannot find a full sentence for X, leave summary_by_item[X] as an empty list and do not invent placeholders.",
      "Extractive summary in Spanish: copy exact spans; DO NOT paraphrase; preserve modality (\"soñaba ser\", \"quería ser\", \"aspiraba a\").",
      "If no relevant info, set {info_found_label}=\"FALSE\", {relevant_context_label}=[], summary_by_item={{}}, {summary_label}=\"\".",
      "NO APOLOGIES, NO FILLER TEXT"
    ]
  },
  "summary_update": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "relevant_context": ["<label keys found>"],
      "summary_by_item": {
        "<label_key>": [
          {"span": "<exact SENTENCE from input text>"}
        ]
      },
      "summary": "<texto in spanish>"
    },
    "instructions": [
      "Previous fields found: {previous_relevant_context}",
      "Previous summary_by_item: {previous_summary_by_item}",
      "If the previous summary above is empty or only whitespace, base {info_found_label}, {relevant_context_label} and {summary_label} only on {input_text_label} (the new document).",
      "If the previous summary above is non-empty, update it by adding or refining victim-relevant facts from {input_text_label}, without removing correct existing details.",
      "If the input is an error/missing page (e.g., \"Página no encontrada\", \"404\", \"no se puede encontrar esa página\"), set {info_found_label}=\"FALSE\", {relevant_context_label}=[], summary_by_item={{}}, {summary_label}=\"\".",
      "Ignore navigation/site chrome (menú, buscar, categorías, compartir, ThemeGrill, WordPress, cookies, copyright).",
      "Use only the current {input_text_label}, {related_context_label} and {previous_summary} to decide the new {info_found_label}, {relevant_context_label} of your document",
      "if {previous_summary_by_item} is not empty, but you do not find any related information in {input_text_label}, keep the previous summary_by_item.",
      "relevant_context should list the keys from {related_context_label} that are found in either {previous_summary} or {input_text_label} (e.g., [\"vic_grupo_social\", \"captura_metodo\", \"perp_tipo1\"]).",
      "For each key X in {relevant_context_label}, check if there is any information about X in {previous_summary} or {input_text_label}. If yes, add one bullet to {summary_label} for X, copying the exact Spanish span(s) from {previous_summary} and/or {input_text_label} about X (no paraphrasing; preserve modality).",
      "For each key X in existing {previous_summary_by_item}, check if there is any new information about X in {input_text_label}. If yes, add the new span(s) to summary_by_item[X] as objects with 'span' (exact text). Keep existing spans from {previous_summary_by_item}.",
      "If no new information is found for X, keep the previous summary_by_item[X] unchanged.",
      "For each key X in the new {relevant_context_label} that is not in {previous_summary_by_item}, extract exact Spanish span(s) from {input_text_label} and store them in summary_by_item[X] as a list of objects with 'span'.",
      "Each span must be a full original sentence ending in sentence punctuation (., ?, !, or equivalent) with no truncation or stitching; if you cannot find a full sentence for X, leave summary_by_item[X] as an empty list and do not invent placeholders.",
      "Extractive summary in Spanish: copy exact spans; DO NOT paraphrase; preserve modality (\"soñaba ser\", \"quería ser\", \"aspiraba a\").",
      "If no relevant info in either {previous_summary} or {input_text_label}, set {info_found_label}=\"FALSE\", {relevant_context_label}=[], summary_by_item={{}}, {summary_label}=\"\".",
      "NO APOLOGIES, NO FILLER TEXT"
    ]
  },
  "classification": {
    "instructions": [
      "OUTPUT FORMAT: Return ONLY {{\"evidence\":\"evidence\", \"result\": \"your_classification\"}}",
      "DO NOT ECHO THE INPUT, QUESTION, OR POSSIBLE_VALUES IN YOUR RESPONSE",
      "Your result MUST be one of the possible_values: {possible_values}",
      "If no information is found about this label, return empty string like {{\"evidence\": \"no information found about this label\", \"result\": \"\"}}"
    ]
  },
  "label_spans_extract": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "spans": [
        {"span": "<exact SENTENCE from input text>"}
      ],
      "confidence_score": "<0.0-1.0>"
    },
    "instructions": [
      "Extract spans from {input_text_label} relevant to label={label} , given the definition: {label_definition}. You are allowed to extract multiple spans from the input text.",
      "If the input is an error/missing page (e.g., \"Página no encontrada\", \"404\", \"no se puede encontrar esa página\"), set {info_found_label}=\"FALSE\", {spans_label}=[], confidence_score=0.0.",
      "Ignore navigation/site chrome (menú, buscar, categorías, compartir, ThemeGrill, WordPress, cookies, copyright).",
      "If you find ANY relevant information for {label}, set {info_found}=\"TRUE\" and extract exact Spanish sentences into {spans}. If you find multiple spans, extract all of them and add them to {spans}.",
      "Each span must be a full original sentence ending in sentence punctuation (., ?, !, or equivalent); no truncation, no stitching, no paraphrasing; preserve modality (\"soñaba ser\", \"quería ser\", \"aspiraba a\").",
      "Set confidence_score to a value in [0,1]. Use higher values when spans explicitly match {label_definition}; lower when weak/ambiguous.",
      "If no relevant information is found, set {info_found_label}=\"FALSE\", {spans_label}=[], confidence_score=0.0.",
      "NO APOLOGIES, NO FILLER TEXT."
    ]
  },
  "label_summary_first": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "spans_by_item": {
        "<label_key>": [
          {"span": "<exact SENTENCE from input text>"}
        ]
      },
      "summary": "<concise summary in spanish>"
    },
    "instructions": [
      "Based on given {spans_by_item}, create a summary.",
      "For each key X in {spans_by_item}, you should have at least one sentence in your summary.",
      "The summary should be a concise summary of the input text, and should be in Spanish. The summary should be no more than 500 words.",
      "If {spans_by_item} is empty or all keys have empty span lists, set {info_found_label}=\"FALSE\" and return an empty summary.",
      "If any key in {spans_by_item} has at least one span, set {info_found_label}=\"TRUE\".",
      "NO APOLOGIES, NO FILLER TEXT"
    ]
  },
  "label_summary_update": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "spans_by_item": {
        "<label_key>": [
          {"span": "<exact SENTENCE from input text>"}
        ]
      },
      "summary": "<concise updated summary in spanish>"
    },
    "instructions": [
      "Previous summary: {previous_summary}",
      "Based on given {spans_by_item} and the previous summary above, create an updated summary.",
      "For each key X in {spans_by_item}, you should have at least one sentence in your summary.",
      "If {previous_summary} is non-empty, incorporate it: keep existing details and add new information with new evidence from {spans_by_item}, without removing what was already known. Refine sentences if necessary to make them more accurate and concise. ",
      "If {previous_summary} is empty or only whitespace, base the summary only on {spans_by_item}.",
      "The summary should be a concise summary in Spanish. The summary should be no more than 500 words.",
      "If {spans_by_item} is empty or all keys have empty span lists, and {previous_summary} is also empty, set {info_found_label}=\"FALSE\" and return an empty summary.",
      "If any key in {spans_by_item} has at least one span, or {previous_summary} is non-empty, set {info_found_label}=\"TRUE\".",
      "NO APOLOGIES, NO FILLER TEXT"
    ]
  },
  "label_synthesis": {
    "output_format": {
      "info_found": "<TRUE|FALSE>",
      "summary": "<synthesized victim-level summary in spanish>"
    },
    "instructions": [
      "Based on given {per_doc_summaries}, which are per-document summaries produced independently for a single victim, create one single coherent victim-level summary.",
      "Summaries may contain duplicates or overlapping information from different documents. Deduplicate and synthesize into a coherent narrative without repetition.",
      "The summary should be concise and in Spanish. No more than 500 words.",
      "If {per_doc_summaries} is empty or all summaries are empty strings, set {info_found_label}=\"FALSE\" and return an empty summary.",
      "If any summary in {per_doc_summaries} is non-empty, set {info_found_label}=\"TRUE\".",
      "NO APOLOGIES, NO FILLER TEXT"
    ]
  }
}