# flow.processors is empty; rejected by FlowConfig.validate_processors_non_empty.
flow:
  schema_version: 1
  name: invalid_missing_steps
  description: Processors list intentionally empty.
  resources:
    - id: default
      type: llm_provider
      provider: openrouter
      model: meta-llama/llama-3.1-70b-instruct
      api_base: https://openrouter.ai/api/v1
      api_key_env: OPENROUTER_API_KEY
  data:
    input_csv: data/df_text_by_report.csv
  taxonomy: config/taxonomy.json
  prompts: config/prompts.json
  processors: []
  output:
    summary_csv: results/fixture/summary.csv
