"""On-disk fixtures shared by :mod:`tests.server` and :mod:`tests.src`.

Kept as plain files rather than inlined literals so the tests stay
readable and the fixture shapes can be diffed over time.
"""
